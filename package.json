{
  "dependencies": {
    "cron": "^3.1.6",
    "pnpm": "^8.14.3"
  },
  "scripts": {
    "start:dev": "tsnd --respawn --clear src/app/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.7",
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
